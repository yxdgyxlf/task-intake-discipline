---
name: task-intake-discipline
description: 会话开局任务接收纪律（强加载）：静候→判别→确认→Skill 检索→开工。
version: 0.1.0
author: yxdgyxlf
license: MIT
platforms: [linux, macos, windows]
metadata:
  hermes:
    tags: [task-intake, session-start, discipline, 强加载, 接收纪律]
---

# 任务接收纪律（Task Intake Discipline）〔强加载〕

会话开局的接收与开工前置协议：任何任务开始前先过本纪律。常驻注入，覆盖一切任务入口。

## 强加载提示词

**本 skill 学习后进入强加载状态：任何任务开始前，强制注入本 skill，作为任务接收与开工的前置协议。**

（载体：skills.auto_load 常态全文注入；生效于后续新会话。）

## 一、四种开局情形

1. **任务明确 + 执行指令明确 + 工作边界明确** → 直接执行。
2. **只写任务，但工作流成熟**（数据与产物链充足完备，几乎不需用户反复纠正、介入）→ 不打断工作流、不加额外环节，自动产出。
3. **只写任务、三无**（无确认指令、无工作边界、无产物提交需求）→ **禁止自行启动**；开局抢跑即此类的错误形态，需确认。
4. **任务与要求模糊**（用户仅有大概方向、需求未定）→ 先确认。

## 二、处置规则

- 情形 1：要加环节（如一次询问），先判定该环节是否必要。
- 情形 2：零额外环节，直接跑完。
- 情形 3、4：宁可等用户逐条给完需求与边界；**明确确认「输入完成」后**，经开工闸门再开始；禁止仅凭部分需求抢先开工（否则用户被迫中途补充需求）。
- 通则：遇问题直接询问用户（弹窗、暂时中断任务）优于自行反复调工具排查——向用户澄清快得多。

## 三、接收时序（开工闸门）

先判别情形类型。任务到 → Agent 只读、静候（零动作、零检索）→ 用户补充指令直到用户明确确认「输入完成」→ 内部快速预演若干方向 → **检索本任务可适用的本机 Skill（防漏载） → 用户明确确认「无新增需求」** → 开工。

- 直通：情形 1、2 判据满足即开工；确认门只走情形 3、4。
- 回流（防空转）：任何环节出现新增需求 → 回到「补充」收口，重新预演与检索后再确认；直到一轮无新增为止。
- 无答复与无人值守：确认门仅作用于用户在场的交互会话（单独 Agent 会话窗口）；无人值守场景（定时任务、子代理、自动化流程）不适用——不等确认，按显式假设执行并标注；交互会话中确认未获答复 → 同一问题只问一次，不重复追问、不擅自推进，静候。
- 盘点要点：含「本机已有、适配度最高的能否升级/新增功能」——技能建设类任务勿上来就「新建」。
- 检索属动作：指令未到不做。

